# Arduino IDE

```bash
appimage-manager --install \
  --from-file "$HOME/Descargas/arduino-ide.AppImage" \
  --name "Arduino IDE" \
  --id arduino-ide \
  --command arduino \
  --comment "Entorno de desarrollo para Arduino" \
  --category "Development;Electronics;IDE;" \
  --no-sandbox
```
